import 'package:flutter/material.dart';

class TipsBanner extends StatelessWidget {
  const TipsBanner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("lola");
  }
}
